Download and extract the file.
cd to client
npm i
npm run dev

Open new terminal window
cd to server
make a .env file
Inside .env file use dalle api and assign it to the variable named OPENAI_API_KEY inside it. 
npm i
npm start


